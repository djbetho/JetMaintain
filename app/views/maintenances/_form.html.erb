<%= form_with(model: maintenance) do |form| %>
  <% if maintenance.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(maintenance.errors.count, "error") %> prohibited this maintenance from being saved:</h2>

      <ul>
        <% maintenance.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  
    <div > 
  <%= form.label :typeengine, style: "display: block" %>
   <select id="tipoMantenimiento" name="tipoMantenimiento"  , :required => true >
            <option value="Preventivo">Motor a reacción</option>
            <option value="Correctivo">Motor turbohélice</option>
</select>
   </div>
 

  <div>
    <%= form.label :dateexecute, style: "display: block" %>
    <%= form.date_field :dateexecute , :required => true %>
  </div>

   <div > 
  <%= form.label :tipoMantenimiento, "Tipo Mantenimiento", style: "display: block" %>
   <select id="tipoMantenimiento" name="tipoMantenimiento"  , :required => true >
            <option value="Preventivo">Preventivo</option>
            <option value="Correctivo">Correctivo</option>
</select>
   </div>

  <div>
 
    <%= form.label :engines_id, "Motor", style: "display: block" %>
    <%= form.collection_select :engines_id, @engines, :id, :name , :required => true%>
  
  </div>

  <div class="mt-3">
    <%= form.label :cities_id, "Ciudad", style: "display: block" %>
    <%= form.collection_select :cities_id, @cities, :id, :name, :required => true%>
  </div>
  <div>
    <%= form.label :users_id, style: "display: block" %>
    <%= form.label current_user.id  %>
  </div>
 
    <div>
      <%= form.label :materials_ids, style: "display: block", class: "form-group" %>
        <div class="row">
        <%= form.collection_check_boxes(:material_ids, @materials, :id, :name) do |b| %>
          <div class="col-md-2">
          <span class="w100 form-control my-2"><%= b.label { b.check_box + " " + b.text }
          %></span>
          </div>
        <% end %>
        </div>
      </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
