<%= form_with(model: maintenance) do |form| %>
  <% if maintenance.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(maintenance.errors.count, "error") %> prohibited this maintenance from being saved:</h2>

      <ul>
        <% maintenance.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :typeengine, style: "display: block" %>
    <%= form.text_field :typeengine %>
  </div>

  <div>
    <%= form.label :namemaintence, style: "display: block" %>
    <%= form.text_field :namemaintence %>
  </div>

  <div>
    <%= form.label :dateexecute, style: "display: block" %>
    <%= form.date_field :dateexecute %>
  </div>

   <div > 
  <%= form.label :tipoMantenimiento, style: "display: block" %>
   <select id="tipoMantenimiento" name="tipoMantenimiento" >
            <option value="Preventivo">Preventivo</option>
            <option value="Correctivo">Correctivo</option>
</select>
   </div>

  <div>
 
    <%= form.label :engines_id, "Engines", style: "display: block" %>
    <%= form.collection_select :engines_id, @engines, :id, :name %>
  
  </div>

  <div class="mt-3">
    <%= form.label :city_id, "Ciudad", style: "display: block" %>
    <%= form.collection_select :city_id, @cities, :id, :name %>
  </div>
  <div>
    <%= form.label :users_id, style: "display: block" %>
    <%= form.text_field current_user.role  %>
  </div>
 
    <div>
      <%= form.label :material_ids, style: "display: block", class: "form-group" %>
      <div class="row">
      <%= form.collection_check_boxes(:material_ids, @materials, :id, :name) do |b|%>
      <div class="col-md-2">
      <span class="w100 form-control my-2"><%= b.label { b.check_box + " " + b.text }
      %></span>
      </div>
      <% end %>
      </div>
      </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
